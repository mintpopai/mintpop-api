name: CI User Portal

on:
  push:
    # 仅分支 push；排除 tag push（发版 tag 走 release workflow，这里无需重复跑）
    branches: ['**']
  pull_request:

concurrency:
  group: ci-user-portal-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  user-portal-quality:
    name: Quality User Portal
    uses: ./.github/workflows/quality-user-portal.yml
